// let divide = () => 2000/100;
let divide = function() {
    return 2000/100
}
let square = x => x * x;
// Don't need a parenthesis if only one parameter
// Use paranthesis only IF two or more parameters
// Max 3 parameters

let add = (x, y) => x + y;
